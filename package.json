{
  "name": "aws-dynamodb-tools",
  "version": "1.0.0",
  "description": "Some AWS DynamoDB wrapper class that support your coding.",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["AWS", "DynamoDB"],
  "author": "n_h",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.721.0",
    "@aws-sdk/lib-dynamodb": "^3.721.0",
    "jest": "^29.7.0"
  }
}
